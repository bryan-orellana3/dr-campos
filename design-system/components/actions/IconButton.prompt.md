Circular icon-only button for secondary utility actions (play, share, close).

```jsx
<IconButton variant="outline" label="Reproducir"><PlayIcon size={18} /></IconButton>
```

Variants solid/outline/ghost; sizes sm(32)/md(40)/lg(48). Always pass `label`.
